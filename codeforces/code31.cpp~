#include <bits/stdc++.h>
using namespace std;

#define MOD1 1000000007
#define MOD2 1000000009
#define MODADD(a,b) (((a) + (b)) % MOD1)
#define MODSUB(a,b,MOD1) (((a) - (b) + (MOD1)) % (MOD1)) // avoid negatives
#define MODMUL(a,b,MOD1) (1LL * (a) * (b) % (MOD1))
#define P 31 // Prime base
#define forn(i, n) for (int i = 0; i < int(n); i++)
class Comparators {
public:
    static bool cmp(pair<int,int> a, pair<int,int> b) {
        return a.second < b.second;
    }
};

class Algorithms {
    // You can add algorithms later here
};

class NumberTeoryAlgo {
public:
    static int gcd(int a, int b) {
        if (b == 0)
            return a;
        return gcd(b, a % b);
    }

    static int lcm(int a, int b) {
        return (a * b) / gcd(a, b);
    }

	static int max_elemens(int a,int b){

		   if(a>b){
			    return a;
		   }else{
	            return b;
		   }
	}
};

class Solution : public Comparators, public Algorithms, public NumberTeoryAlgo {
public:
    static solve(vector<int>vs) {

		   int count=0;
		   for(int i=0;i<vs.size()-1;i++){

			     if(vs[i]%2==1){

					  swap(vs[i],vs[i+1]);
					  count++;
				 }else{

					  count=0;
				 }
		   }

		  cout<<count/2<<endl; 
	}

	static int maxof(vector<int>arr){

		 int max_int=arr[0];

	   for(int i=0;i<arr.size();i++){

		    if(arr[i]>max_int){

                 max_int=arr[i];
			}
	   }

	   return max_int;
	}

	static void  missingNum(vector<int>& arr) {

	      	

   }

    static void inputarr() {

		  int n;
		   vector<int>vs2;
		   cin>>n;
		   for(int i=0;i<n;i++){
			  int m;
			  cin>>m;
			  vs2.push_back(m);
		   }

            missingNum(vs2);		   
        }
   static void inputsingle(){
    
	 }
};
int main() {
   //  freopen("input.txt", "r", stdin);
   //  freopen("output.txt", "w", stdout);
   
		Solution::inputarr();

}
